{
  "name": "holochain-ui",
  "version": "1.0.0",
  "description": "A Holochain app (hApp) for managing your personal private information.",
  "scripts": {
    "hc:build": "echo `pwd` && rm -rf tmp-storage && mkdir -p dna && cd dna-src && ../../holochain/hc package --strip-meta -o ../dna/personas-profiles.dna.json",
    "hc:start": "mkdir -p tmp-storage && ../holochain/holochain -c ./conductor-config.toml",
    "hc:test": "cd dna-src && ../../holochain/hc test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://willemolding@github.com/holochain/holochain-ui.git"
  },
  "author": "",
  "bugs": {
    "url": "https://github.com/holochain/holochain-ui/issues"
  },
  "homepage": "https://github.com/holochain/holochain-ui#readme",
  "dependencies": {
  }
}
